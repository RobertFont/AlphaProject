using System;
using System.Collections;
using System.Collections.Generic;
/*using UnityEngine;
using UnityEditor;
using UnityEngine.UI;

[CustomEditor(typeof(InteractiveUITriggers))]
public class InteractiveUITriggersEditor : Editor
{
    enum UIElementType
    {
        Button=0,
        Toggle,
        Slider
    }

    UIElementType uIElement;

    int toggleValue;
    float sliderValue;

    void OnEnable()
    {
        //toggleValue = serializedObject.FindProperty("value");
        //sliderValue = serializedObject.FindProperty("sliderValue");
    }

    public override void OnInspectorGUI()
    {
        serializedObject.Update();

        uIElement = (UIElementType)EditorGUILayout.EnumPopup("UI Element", uIElement);

        if(uIElement == UIElementType.Slider)
            sliderValue = EditorGUILayout.Slider(new GUIContent("Slider"), sliderValue, 0.0f, 1.0f);
        
    }
}*/
